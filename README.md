Used Github copilot 
# Users UI App

This repository contains a minimal Angular 18 single-page application with two navigation screens:

- Add User (form with validation)
- List Users (fetches from API)

APIs used:
- GET /api/getusers
- POST /api/postuser

To run locally:

1. npm install
2. npm start

To run tests:

npm test
